# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
venv/
.venv/
ENV/
env/

# Sensitive user config & personal API keys
data/config.json
*.env
.env.*

# SQLite WAL files
data/*.db-wal
data/*.db-shm
data/*.db-journal

# IDE & OS files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.DS_Store
Thumbs.db
Desktop.ini

# Yerel bellek veritabanı (kişisel sohbet geçmişi + API anahtarı içerir, repoya girmez)
data/mehbur_memory.db
data/*.db

# 👁️ Kamera görüntüleri (görsel anlama + /foto, kişisel, repoya girmez)
data/camera_captures/
data/vision_captures/
data/security_snapshots/

# Telegram uzaktan kontrol — geçici ekran görüntüleri (kişisel, repoya girmez)
data/remote_captures/

# Sesli sohbet — indirilen Vosk Türkçe modeli (~35 MB, ilk açılışta iner)
data/models/

# Uygulama ikonu — assets/logo.png'den otomatik üretilir
assets/logo.ico

# .exe paketleme (PyInstaller) çıktıları — MehburAI.spec ile yeniden üretilir,
# repoya girmez (yüzlerce MB ikili dosya)
build/
dist/

# Üretilen / düzenlenen görseller (kişisel, repoya girmez)
data/generated_images/
