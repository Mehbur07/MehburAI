# MehburAI - Bağımlılıklar
# ========================

# Masaüstü Arayüzü (GUI)
customtkinter>=5.2.0
Pillow>=10.0.0

# HTTP istekleri — Gemini REST API + Wikipedia + Telegram bildirimi
requests>=2.31.0

# 🛡️ Güvenlik Modu — web kameradan fotoğraf yakalama
# (opsiyonel: kurulu değilse güvenlik modu yalnızca Telegram metin uyarısı gönderir)
opencv-python>=4.9.0

# 🛡️ Güvenlik Modu — sistem tepsisi (pencere kapatılınca arka planda çalışma)
# (opsiyonel: kurulu değilse pencere kapatınca simge durumuna küçülür)
pystray>=0.19.0

# 🎙️ Sesli Sohbet — "Mehbur" uyandırma sözcüğü + doğal Türkçe seslendirme +
# Telegram sesli mesaj → yazı. (opsiyonel: kurulu değilse sesli sohbet kartı
# devre dışı görünür, diğer her şey çalışır.)
#   vosk       → çevrimdışı Türkçe konuşma tanıma (ilk açılışta ~35 MB model iner)
#   sounddevice→ mikrofon kaydı + hoparlörden oynatma
#   soundfile  → ses dosyası çözme (mp3 / ogg-opus / wav)
#   edge-tts   → Microsoft Edge doğal Türkçe TTS (Emel / Ahmet sesleri)
vosk>=0.3.45
sounddevice>=0.4.6
soundfile>=0.12.1
edge-tts>=6.1.0
numpy>=1.24.0

# ─────────────────────────────────────────────
# NOT: Eski `google-generativeai` paketi Google tarafından kullanımdan
# kaldırıldı ve MehburAI artık Gemini'ye doğrudan REST (streamGenerateContent)
# üzerinden bağlanıyor. Ekstra bir SDK gerekmiyor.
#
# Aşağıdakiler şu an kod tarafından kullanılmıyor; gelecekteki gömülü
# (embedding) tabanlı semantik arama için opsiyoneldir:
# sentence-transformers>=3.0.0
# scikit-learn>=1.3.0
# numpy>=1.24.0
