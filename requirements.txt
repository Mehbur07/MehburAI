# MehburAI - Bağımlılıklar
# ========================

# Masaüstü Arayüzü (GUI)
customtkinter>=5.2.0
Pillow>=10.0.0

# HTTP istekleri — Gemini REST API + Wikipedia
requests>=2.31.0

# ─────────────────────────────────────────────
# NOT: Eski `google-generativeai` paketi Google tarafından kullanımdan
# kaldırıldı ve MehburAI artık Gemini'ye doğrudan REST (streamGenerateContent)
# üzerinden bağlanıyor. Ekstra bir SDK gerekmiyor.
#
# Aşağıdakiler şu an kod tarafından kullanılmıyor; gelecekteki gömülü
# (embedding) tabanlı semantik arama için opsiyoneldir:
# sentence-transformers>=3.0.0
# scikit-learn>=1.3.0
# numpy>=1.24.0
